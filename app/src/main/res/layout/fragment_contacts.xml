<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--<include layout="@layout/header_rv_contacts"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:id="@+id/contact_header"/>

    <ListView
        android:id="@+id/rvContacts"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/contact_header"
       />

   &lt;!&ndash; app:rv_divider_color="@color/line"
    app:rv_divider_size="1px"&ndash;&gt;

    <com.example.lidongxue.chat.widget.QuickIndexBar
        android:id="@+id/qib"
        android:layout_width="40px"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_below="@id/contact_header"/>
    &lt;!&ndash;字母对话框&ndash;&gt;
    <TextView
        android:id="@+id/tvLetter"
        android:layout_width="150px"
        android:layout_height="150px"
        android:layout_centerInParent="true"
        android:background="@drawable/shape_contact_letter_bg"
        android:gravity="center"
        android:text="A"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:visibility="gone"/>-->

</RelativeLayout>